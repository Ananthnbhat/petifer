import React from 'react';
import { Text } from 'react-native';

import matchedPets from '../assets/test/matched-pets.json'

const MatchedPets = () => {
  return <Text>Open this page to show matched pets</Text>;
};

export default MatchedPets;
